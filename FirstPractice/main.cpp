#include <iostream>
#include <math.h> // Libreria de mate
using namespace std;

int main() {
    int num,a ;

    cout << "Deme un numero: ";
    cin >> num;
    cout << "El numero es:" << num;
    a = sqrt(num); // Raiz
    a = pow(num, 2); // Elevacion


    return 0;
}
